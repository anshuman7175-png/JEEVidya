Pillow>=10.0.0
matplotlib>=3.7.0
moviepy>=1.0.3
google-genai>=0.2.0
edge-tts>=6.1.9
numpy>=1.24.0
Flask>=3.0.0
pydub>=0.25.1
rembg[cpu]>=2.0.50
onnxruntime>=1.16.0
mcp[cli]>=1.27,<2
imageio-ffmpeg>=0.4.9
audioop-lts>=0.2.1; python_version >= "3.13"
# Auto-rigging: mediapipe face landmarks give pixel-accurate face geometry.
# Without it the rig builder falls back to a proportion heuristic that is
# badly wrong for chibi characters (mouth lands on the eyes).
# The GUI OpenCV build needs libxcb/X11 which headless servers lack —
# ALWAYS use the -headless variant.
mediapipe>=1.0.0
opencv-contrib-python-headless>=4.9
