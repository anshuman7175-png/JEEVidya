Pillow>=10.0.0
matplotlib>=3.7.0
moviepy>=1.0.3
google-genai>=0.2.0
edge-tts>=6.1.9
numpy>=1.24.0
Flask>=3.0.0
pydub>=0.25.1
rembg[cpu]>=2.0.50
# rembg → pymatting → numba: without this floor the resolver picks the
# ancient numba 0.53, which refuses to install on Python >= 3.10 and
# kills the whole dependency resolution. Pin it forward explicitly.
numba>=0.61; python_version >= "3.10"
onnxruntime>=1.16.0
mcp[cli]>=1.27,<2
imageio-ffmpeg>=0.4.9
audioop-lts>=0.2.1; python_version >= "3.13"
# Auto-rigging: mediapipe face landmarks give pixel-accurate face geometry.
# Without it the rig builder falls back to a proportion heuristic that is
# badly wrong for chibi characters (mouth lands on the eyes).
#
# GOTCHA: mediapipe declares a dependency on the GUI opencv-contrib-python,
# which needs libxcb/X11 that headless servers lack (cv2 import dies with
# "libxcb.so.1: cannot open shared object file"). After installing this
# file, ALWAYS run the two-step fix (also detected by `jvmake doctor`):
#   pip uninstall -y opencv-contrib-python
#   pip install --force-reinstall opencv-contrib-python-headless
# GOTCHA 2: the OpenCV 5.0.0.x wheels install without error but ship no
# importable `cv2` on this platform — `import cv2` raises ModuleNotFound
# while pip/uv report the package as satisfied. That combination is
# especially nasty because it looks like a code bug, not an install bug.
# The 4.x series is the last one known to import cleanly, so the major
# version is CAPPED rather than left open.
mediapipe>=1.0.0
opencv-contrib-python-headless>=4.9,<5
